import{describe,expect,it}from"vitest";import{assertTicketTransition,canTransitionTicket,nextNotificationAttempt,notificationKey}from"../operations-domain";
describe("operations domain",()=>{it("permite el ciclo operativo controlado",()=>{expect(canTransitionTicket("received","triage")).toBe(true);expect(canTransitionTicket("in_progress","resolved")).toBe(true);expect(canTransitionTicket("resolved","closed")).toBe(false);});it("rechaza saltos de estado",()=>expect(()=>assertTicketTransition("received","closed")).toThrow());it("genera claves idempotentes estables",()=>expect(notificationKey("ticket","abc","status:triage","user")).toBe("ticket:abc:status:triage:user"));it("aplica backoff con límite",()=>{const now=new Date("2026-01-01T00:00:00Z");expect(nextNotificationAttempt(0,now).toISOString()).toBe("2026-01-01T00:05:00.000Z");expect(nextNotificationAttempt(20,now).toISOString()).toBe("2026-01-02T00:00:00.000Z");});});